# 吉林大学体育馆前端仿制

课程学习用的纯前端项目，仿制吉林大学“智慧场馆客户服务管理平台”的移动端界面。
Vue 3 + Vue Router + Vite，**不连接任何后端**，不执行原站代码，不请求原站接口。

## 快速开始

```bash
npm install
npm run dev
```

浏览器打开终端输出的地址，默认进入场馆选择页（`#/venues`）。

## 命令

| 命令 | 说明 |
| --- | --- |
| `npm run dev` | 启动开发服务器 |
| `npm test` | 运行全部测试（Node 原生测试 + Vitest） |
| `npm run build` | 生产构建，输出到 `dist/` |

## 页面

| 路由 | 页面 |
| --- | --- |
| `#/venues` | 场馆选择 |
| `#/home/:venueId` | 场馆首页（`song` 宋治平 / `qianwei` 前卫） |
| `#/reserve/:venueId` | 场地预约项目 |
| `#/profile` | 个人中心 |
| `#/my-bookings` | 我的预约 |
| `#/my-bookings/detail` | 预约详情与入场码 |

未知路由与未知场馆 ID 都会安全回退（分别回到场馆选择页和宋治平体育馆）。

## 核心功能：演示预约 + 静态入场码

这是本项目最主要的功能，完整链路都在浏览器里跑通：

1. `#/venues` 点击平台 Logo → 打开「预约设置」，选择体育馆、项目、日期、时间段、场地号。
2. 点「保存预约」→ 写入 `sessionStorage` → 弹出「保存成功」。
3. `#/profile` →「我的预约」→ 看到订单卡片（订单号 / 场地 / 数量 / 购买时间），可「取消预约」。
4. 「查看详情」→ 右上角二维码图标 → 弹出「入场码」票券。

> **入场码是静态的**：`src/assets/original/fixed-entry-qr.png` 是一张固定的本地图片，
> 每次打开都是同一张，不包含订单信息，也不经过任何服务端校验。
> 它只能在这个本地演示里显示，**不是真实凭证，不能用于真实场馆的入场核验**。

## 数据说明

- 场馆、项目、个人资料全部来自 `src/data/venues.js` 的本地静态数据。
- 预约是**演示用的假数据**：点击场馆选择页的 Logo 可打开「预约设置」，保存后写入浏览器
  `sessionStorage`（键 `jlu-gym-mock-booking`），关闭标签页即失效。
- 入场二维码是一张固定的本地占位图片，**不代表任何真实入场权限，不能用于任何真实场馆核验**。
- 不实现登录、实名认证、支付、购票和真实预约。

## 素材来源

`src/assets/original/` 中的图片提取自原站公开前端，仅在本地使用，运行时不请求原站。
原站 HTML / CSS / Webpack 分包副本（`reference-source/`）**未包含在本仓库中**，它只用于本地对照，
并且可以随时重新下载：

```bash
node scripts/extract-original-assets.mjs   # 需要网络，写入 reference-source/ 与 src/assets/original/
```

## 更多文档

- [docs/页面说明.md](docs/页面说明.md)：每个页面的内容、可点击区域与演示路径
- [docs/技术说明.md](docs/技术说明.md)：目录结构、数据模型、组件、测试与限制

> 本项目仅用于课程学习与界面练习，与原站及其运营方无关。
